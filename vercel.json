{
  "buildCommand": "prisma migrate resolve --applied 20260516144120_init || true && prisma migrate deploy && prisma generate && next build --webpack",
  "crons": [
    {
      "path": "/api/cron/expire-orders",
      "schedule": "0 2 * * *"
    }
  ]
}
